[package]
name = "sbunix"
version = "0.1.0"
edition = "2021"

[lib]
path = "kernel/kernel.rs"
crate-type = ["staticlib"]

[profile.dev]
panic = "abort"
opt-level = "s"

[profile.release]
panic = "abort"
