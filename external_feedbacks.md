Feedback:
When a POSIX syscall like open fails, the convention is twofold: the function returns -1, and the global errno variable is set to a positive error code indicating what went wrong (for example, ENOENT when a file does not exist). Your OS kernel correctly produces negative error codes internally, but the user-space library does not translate those raw negative values into the standard errno convention for all syscalls. Some of your libc wrappers do perform this translation, but others pass the raw kernel return value straight through to the caller. This means a program that checks errno after a failed open will not see the expected error code.

To investigate, try writing a small program that opens a path that does not exist, then prints both the return value from open and the value of errno. According to POSIX, you should see a return of -1 and errno set to a specific "no such file" constant. If you instead see a small negative number as the return value and errno unchanged, the translation layer between the kernel and user space is missing for that particular call. Look at how your libc handles error returns for syscalls that do work correctly with errno and apply the same pattern consistently.


Feedback:
A freestanding OS needs its own C library that exposes standard POSIX headers so that programs written against well-known interfaces can compile and link successfully. Your OS currently provides a reasonable set of headers covering basic I/O, process control, string operations, time structures, and file control, but there are significant gaps in both the set of headers available and the declarations within existing headers.

Some standard headers are expected at paths under a sys/ subdirectory. Your libc defines time-related structures in a top-level header, but programs that include the conventional sys/ variant for time cannot find them because that sys/ header does not exist. Similarly, there is no header for memory mapping or error reporting, and no header for directory traversal. Beyond missing header paths, several of the headers you do provide are missing standard function declarations that POSIX programs rely on. For example, your process and I/O header does not declare functions for querying process identity, changing directories, querying the current working directory, sleeping, seeking within files, or removing files. Your string header is missing a standard string duplication function. Your standard library header does not declare memory allocation or string-to-integer conversion functions. Your signal header does not define the bitmask type used for signal sets or the structure and function used for advanced signal handler registration. Your wait header is missing constants for non-blocking and stop-aware waiting.

Your kernel and shell clearly work to a significant degree (echo, exit codes, and the boot sequence all function), so the gap is primarily in how your libc presents its interface. Try compiling a small program that calls common POSIX functions (querying process identity, duplicating strings, allocating memory, registering signal handlers with masks, waiting with options) and see which headers and declarations are missing. The pattern for adding new declarations is the same as what you already have; it is largely a matter of completeness. 

Feedback:
When a process replaces its image via exec, the new program runs to completion and its exit status must be faithfully reported to the parent through waitpid. This includes the case where a shell is invoked with a flag to run a single command string; the shell must propagate the exit status of that command back to its own caller. Your OS can exec programs and retrieve exit codes in straightforward cases, but when the shell is asked to run a builtin command like exit with a numeric argument, the requested exit code is not honored. The shell always terminates with the same status regardless of what argument was passed to the exit builtin. Try writing a small program that forks, execs the shell with a command string containing exit followed by various numeric codes, and prints the value returned by waitpid. You should see the requested code come back each time, but right now you will not.

Feedback:
When a process replaces its image via exec, the new program runs to completion and its exit status must be faithfully reported to the parent through waitpid. This includes the case where a shell is invoked with a flag to run a single command string; the shell must propagate the exit status of that command back to its own caller. Your OS can exec programs and retrieve exit codes in straightforward cases, but when the shell is asked to run a builtin command like exit with a numeric argument, the requested exit code is not honored. The shell always terminates with the same status regardless of what argument was passed to the exit builtin. Try writing a small program that forks, execs the shell with a command string containing exit followed by various numeric codes, and prints the value returned by waitpid. You should see the requested code come back each time, but right now you will not.

Feedback:
A Unix libc must provide standard POSIX structure definitions so that external programs can compile against it. POSIX specifies certain structures that are used in time-related system calls, and every header that references those structures must use the actual structure type rather than an opaque pointer. Your OS's libc currently omits the definition of a standard structure that POSIX time-related interfaces require, and the functions that should accept a pointer to that structure instead accept a generic void pointer. This type mismatch means that when external programs (or their build shims) declare the same functions using the proper POSIX-conformant types, the declarations conflict with what your headers provide. This is why none of the external programs (the interpreter, the compiler, the build tool, the utility suite) can be compiled against your libc. Review the POSIX specification for time-of-day related interfaces and ensure that all structures those interfaces reference are properly defined in your headers, and that your function declarations use the correct structure pointer types rather than void pointers.

Feedback:
POSIX defines several named constants that control signal handler behavior. These constants are stored in the flags field of the sigaction structure and govern things like whether interrupted ystem calls should be restarted and whether the handler should reset to default after one delivery. Your OS defines the sigaction structure in its user-space signal header and includes the flags field, but it does not define any of the standard flag constants that a program would store in that field. Without these constants, any portable C code that registers signal handlers with specific behavioral options cannot compile against your libc. This prevents third-party programs from being built. The kernel already implements support for these flags internally; the gap is simply that the user-facing header does not expose the corresponding constant definitions to user-space programs.

Feedback:
For third-party C programs to compile against your libc, your headers must communicate the full contract of each function to the compiler, not just its parameter and return types. Some standard library functions are defined by POSIX and the C standard as never returning to their caller. The compiler uses this information for optimization and correctness analysis; when it cannot verify that a calling function truly never returns, it may reject the code. Your OS currently declares certain process-termination functions without informing the compiler that they never return. As a result, third-party code that relies on the compiler knowing about this "never returns" property fails to build. Review how the C standard specifies that functions which unconditionally terminate a process should be annotated, and ensure your headers convey this property

Feedback:
When a process sends a fatal signal to its own process group (using a negative pid), POSIX requires that every process in the group receive the signal. Your OS correctly handles sending signals to individual processes, and it handles most signal types properly. However, sending a fatal signal to a process group does not reliably reach all members. Consider what happens when the sender itself belongs to the target group: if the kernel terminates the sender partway through iterating the group members, the remaining members never receive the signal. A correct implementation must ensure that all members of the group are marked for termination before any one of them (including the sender) actually exits. Try writing a small program that forks several children into a shared process group, then has the parent call kill with a negative pgid and a fatal signal; verify that all children are terminated, not just the ones that happen to appear earlier in the process table.

POSIX signal masking allows a process to temporarily block specific signals so they accumulate as pending rather than being delivered. When a signal is added to the process's signal mask, any subsequent occurrence of that signal must remain pending (not invoke the handler) until the mask is modified to unblock it. Your OS correctly installs signal handlers. It also correctly delivers signals and dispatches to user handlers for the basic cases. However, when a process explicitly blocks a signal and then sends that signal to itself, the handler appears to fire before the blocking call's effect is respected. To investigate, try writing a small program that blocks a signal, sends it to itself, checks a volatile flag (which the handler would set), then unblocks the signal and checks the flag again. The flag should only become set after unblocking, never before.


Feedback:
A Unix-like OS needs its user-space C library to expose standard POSIX headers so that programs written against well-known interfaces can compile and link successfully. Your OS currently provides a small set of headers (covering I/O, process control, signals, terminal control, and file control), but it is missing several that are expected by the POSIX standard, most critically the header for string and memory operations. Your libc does contain implementations of string functions in a source file, but without the corresponding header file declaring them, no user-space program that includes the standard string header can compile. Similarly, headers for wait status macros and time-related structures under the conventional sys/ subdirectory paths are absent.

The good news is that your kernel and shell clearly work to a significant degree; echo and exit codes function correctly, which means the core syscall mechanism, process lifecycle, and basic I/O are operational. The gap is purely in how your libc presents its interface. Review what standard POSIX headers a typical C program includes and ensure your libc provides at least the subset needed by this OS. Try compiling a simple program that includes common headers for string operations, wait macros, and time structures against your libc and see which ones are missing. The pattern for adding new headers is straightforward: declare the types, constants, and function prototypes that correspond to the implementations you already have.

Feedback:
When a POSIX syscall like open fails, the convention is twofold: the function returns -1, and the global errno variable is set to a positive error code indicating what went wrong (for example, ENOENT when a file does not exist). Your OS kernel correctly produces negative error codes internally, but the user-space library does not translate those raw negative values into the standard errno convention for all syscalls. Some of your libc wrappers do perform this translation, but others pass the raw kernel return value straight through to the caller. This means a program that checks errno after a failed open will not see the expected error code.

To investigate, try writing a small program that opens a path that does not exist, then prints both the return value from open and the value of errno. According to POSIX, you should see a return of -1 and errno set to a specific "no such file" constant. If you instead see a small negative number as the return value and errno unchanged, the translation layer between the kernel and user space is missing for that particular call. Look at how your libc handles error returns for syscalls that do work correctly with errno and apply the same pattern consistently.

Eval 01: A freestanding OS needs its own C library that exposes the standard POSIX programming interface to user programs. This means the libc must provide the full set of header files that POSIX programs expect to include, along with the type definitions, constants, and function declarations those headers contain. Your OS currently provides only a small handful of headers covering basic I/O, string operations, and process control. It does not provide headers for time operations, file control flags, signal handling, directory access, or wait status macros, among others. Without these headers, no user program that relies on standard POSIX interfaces can compile against your libc.

Beyond just creating the header files, each header needs to declare the appropriate types (such as time-related structures), constants (such as file open flags or signal numbers), and function prototypes. Some of those functions also need implementations in your libc source. You already have a working syscall mechanism and a few wrapper functions; the pattern for adding new ones is the same. Review what standard POSIX headers a typical C program includes and ensure your libc provides at least the subset needed by this OS. A good exercise is to try compiling a simple program that calls common POSIX functions (getpid, open with creation flags, time, kill, malloc) and see which headers and declarations are missing.
score: 21

Eval 02: A freestanding OS needs its own C library that exposes the standard POSIX programming interface to user programs. This means the libc must provide header files at the paths that POSIX programs expect to include, with the type definitions, constants, and function prototypes those headers are supposed to contain. Your OS provides a reasonable set of headers covering basic I/O, process control, string operations, and time, but there are gaps in both the set of headers available and the declarations within existing headers.

Some standard headers are expected at paths under a sys/ subdirectory (for example, time-related structures and functions that POSIX specifies in a sys/ header, or syscall number definitions at the conventional sys/ path). Your libc defines these things, but places them only at the top-level path, so programs that include the sys/ variant cannot find them. Beyond missing header files, several of the headers you do provide are missing standard function declarations and type definitions that POSIX programs rely on. For example, your string header is missing some standard memory and string duplication functions; your process/IO header is missing a terminal-query function; your signal header does not define the conventional function-pointer typedef that many programs use for signal handlers; and your environment support is missing functions to modify or remove environment variables.

A good exercise is to try compiling a small program that calls common POSIX functions (time queries, string duplication, terminal checks, environment modification, process group manipulation) and see which headers and declarations are missing. The pattern for adding new declarations is the same as what you already have; it is largely a matter of completeness.
score: 20

Eval 03: A POSIX-like OS needs to report the current wall-clock time as seconds since the Unix epoch (January 1, 1970). This is distinct from simply counting how many timer interrupts have occurred since the system booted. Your OS currently reports uptime (seconds since boot) when a program queries the real-time clock, which means the returned value is a small number like 2 or 5 rather than a proper epoch timestamp in the billions. The QEMU virt machine provides a hardware real-time clock that knows the actual calendar date and time; the kernel needs to read from this device to obtain real-world wall-clock time. Try writing a small program that calls the standard time-querying interface and prints the result. If the value is not a plausible date (for example, a number greater than one billion, corresponding to dates after 2001), then the kernel is not sourcing its time from the right place.
score: 26

Eval 04: POSIX specifies that /bin/sh must be the standard command interpreter on any conforming system. Programs, scripts, and other components of the OS all rely on this path existing and being a working shell. Your OS's boot script uses a non-standard interpreter path in its shebang line rather than the conventional /bin/sh, and the root filesystem does not provide /bin/sh at all (neither as a binary nor as a symbolic link). This means any program that tries to read the boot script will find unexpected content, and any program that tries to invoke /bin/sh will fail. To fix this, ensure the root filesystem provides /bin/sh (a common approach is to create a symbolic link from /bin/sh to whatever shell binary the OS actually uses) and update any scripts to reference the standard path.
score: 50

Eval 05: A kernel must be defensive: when userspace passes arguments that do not make sense, the kernel should not silently succeed. It should clearly tell the caller that the request was invalid. Think about what happens in your kernel when a program makes a request using a resource handle it does not actually hold. Does the kernel distinguish between "nothing happened because there was nothing to do" and "the request itself was nonsensical"? These two situations look very different to a well-written program. Write a small program that deliberately makes a few bogus requests to the kernel and inspect what comes back; consider whether a caller could tell apart a legitimate empty result from an error.
score: 57

Eval 06: A POSIX-like OS must support copy-on-write (COW) semantics so that when a process forks, both parent and child can continue to read and write to their memory independently. The kernel marks shared pages as read-only after fork; when either process writes to such a page, the kernel's page fault handler should transparently allocate a private copy and let the write proceed. Your OS successfully allocates and manages memory for a single process (heap allocation, reallocation, freeing, and zero-initialization all work), but when a process forks and either the child or parent attempts to write to inherited memory, the kernel terminates both processes with an unhandled store page fault instead of resolving the fault.

The issue lies in how the kernel's page fault handler resolves write faults on pages that were shared during fork. The handler needs to recognize that a store fault on a page marked for copy-on-write should trigger a page copy rather than a process kill. Try writing a small program that allocates a buffer, writes a known value, forks, then has the child overwrite the buffer and exit. If the child crashes on the write, the fault handler is not recognizing or resolving the COW condition on that page. Investigate the path from the store fault through to the handler that checks for the COW marker on the page table entry.
score: 23

Eval 07: Your basic memory operations (malloc, free, realloc) work correctly within a single process, but after a while, processes that allocate memory start to fail. At that point, cascading failures across all running processes happen. To investigate, try writing a program that repeatedly forks a child, has the child allocate and touch some memory, then has the child exit. The parent should wait for each child before forking the next.
score: 35

Eval 08: When the kernel allocates physical pages for a new process (for example, to set up its stack during program loading), POSIX requires that those pages contain zeroes. This is both a correctness requirement (the initial stack layout depends on specific zero values) and a security requirement (stale data from a previous process must not leak). Your OS correctly handles this in one of its program-loading paths but not the other. The result is that after certain fork-and-exit workloads dirty physical pages and return them to the free pool, programs loaded through the affected path receive stack pages filled with stale data from previous processes. This corrupts the initial program state (argc, argv, environment pointers) so thoroughly that the program produces no output at all, even though the program itself is correct.

The key symptom is that your OS runs the first few programs after boot successfully, but after a workload that forks children which write to memory and then exit, every subsequently launched program silently fails to produce any output. The shell remains functional (builtins like echo and pipes still work), but external programs appear broken. This is not a libc bug; it is a kernel memory management issue. To investigate, try writing a small program that forks a child, has the child allocate and write to a large buffer, exits the child, then has the parent fork and exec a simple hello-world program. If the hello-world program fails after this sequence but works on a fresh boot, the kernel is handing out recycled physical pages without clearing them first. Ensure that every code path that allocates physical pages for user-space mappings provides zeroed memory.
score: 27

Eval 09: POSIX requires that anonymous memory obtained through memory mapping be zero-initialized. This guarantee must hold even when the underlying physical pages were previously used by another process; the OS must ensure every recycled page is scrubbed before it becomes visible to user space. Your OS appears to have zeroing logic in the page allocator, but freshly mapped anonymous memory at certain virtual addresses is not being delivered as zeroed pages. Instead, the process is terminated when it attempts to read the mapping.

The issue relates to how the user-space virtual address space is laid out. An OS must ensure that the range of virtual addresses used for user memory mappings does not overlap with any region the kernel has already claimed for its own purposes in the same page table. If a user mapping lands at an address where the kernel already installed entries for device I/O or other internal use, the demand-paging logic may find pre-existing page table entries and fail to allocate a fresh page. Try writing a small program that requests anonymous memory at various addresses (both low and high in the user address range) and checks whether the returned pages are accessible and contain all zeros. Pay attention to which addresses succeed and which cause the process to crash.
score: 26

Eval 10: The kernel's physical memory allocator must maintain a consistent view of which pages are free and which are in use. When a child process allocates and writes to a large amount of memory (100MB), the kernel needs to provide physical pages on demand and track their ownership correctly throughout their lifetime. Your OS currently has a defect where the same physical page is being released back to the free pool more than once. This corrupts the allocator's internal state, and subsequent allocations may hand out a page that is still in use elsewhere, leading to memory access faults that crash the child process instead of letting it exit cleanly.

The issue arises because your OS has multiple code paths that can free the same physical page, and they are not properly coordinated. When memory pressure is high and page reclamation runs alongside normal process activity, a page may be freed by one path and then freed again by another, neither of which realizes the other has already acted. To investigate, try forking a child that allocates and writes to progressively larger amounts of memory (say 30MB, then 60MB, then 100MB) and observe at what threshold the child begins crashing. This will help you isolate whether the issue is specifically triggered by memory pressure and narrow down which page lifecycle paths are racing with each other.
score: 63


NOTE: getpid should work
	 aim for posix compliance