/*
 * true — return 0 (success), no output. POSIX standard utility.
 *
 * Useful as a placeholder in shell logic: `cmd && true` succeeds,
 * `while true; do …` loops forever, etc.
 */
int main(void) { return 0; }
